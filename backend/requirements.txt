Django~=5.2
djangorestframework~=3.15
djangorestframework-simplejwt~=5.3
python-dotenv~=1.0
psycopg2-binary~=2.9
Pillow~=11.0
uuid6>=2024.7  # UUIDv7 generator (native uuid.uuid7 only lands in Python 3.14)

# Receipt parsing — OpenAI-compatible client for the self-hosted vision LLM
openai>=1.40
# Render PDF receipts to image (page 1) before sending to the vision model
PyMuPDF>=1.24
django-filter~=24.3
django-axes~=6.5
# Block throwaway/disposable email domains at registration (maintained list).
disposable-email-domains>=0.0.100
# TOTP two-factor auth (Django admin + API users).
django-otp~=1.5
qrcode[pil]~=8.0

# Background tasks / scheduling.
# Receipt parsing needs an on-demand, single-concurrency, retryable queue.
celery[redis]~=5.4
redis~=5.2
django-celery-beat~=2.7

# Production WSGI server (Docker / demo)
gunicorn~=23.0

pytest~=8.3
pytest-django~=4.9
pytest-cov~=5.0
factory-boy~=3.3
pytest-factoryboy~=2.7
